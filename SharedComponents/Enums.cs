namespace SharedComponents;
public enum AlertStyle
{
    Primary,
    Secondary,
    Success,
    Danger,
    Warning,
    Info,
    Light,
    Dark
}

public enum ButtonType
{
    Save,
    Cancel,
    Delete,
    Remove,
    Select
}